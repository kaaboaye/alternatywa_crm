<h2><%= @title = "#{t :recommendation_for} #{@recommendation.member.full_name}" %></h2>

<%= link_to t("activerecord.models.member"), member_path(@recommendation.member) %> |
<%= link_to t(:recommendations), member_recommendations_path(@recommendation.member) %> |
<%= link_to t(:add_recommendation), new_member_recommendation_path(@recommendation.member) %>

<%= render "form" %>
